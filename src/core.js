/**
 * Dummy function, does nothing.
 *
 * @method dummy
 * @memberOf ran.core
 * @param {number} x Parameter.
 * @return {number} Some dummy value.
 */
function dummy(x) {
    return Math.random() * x;
}